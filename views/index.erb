<h2>Welcome to Pizza Shop!</h2>
<div class="row">
	<% @products.each do |p| %>
	<div class="col-md-4">
		<div>
			<img src="<%= p.path_to_image %>">	
		</div>
		<div>
			<%= p.title	%>
		</div>
		<div>
			<%= p.description %>
		</div>

	</div>
	<% end %>
</div>